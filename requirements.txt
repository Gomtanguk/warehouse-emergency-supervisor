# pip requirements for warehouse-emergency-supervisor
# ROS 2 packages, Isaac Sim, omni/pxr modules, and simulation runtime
# dependencies are expected to be provided by the target environment.

numpy>=1.23
opencv-python>=4.8
Pillow>=10.0
PyYAML>=6.0
pyzbar>=0.1.9
